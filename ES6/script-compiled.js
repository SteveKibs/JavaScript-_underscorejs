let counts = 10,
    price = 0.25,
    messages = `${counts} items cost $${(counts * price).toFixed(2)}.`;

console.log(messages);

let name = "Nicholas",
    message = `Hello, ${`my name is ${name}`}.`;
console.log(message);
